name: hello world
on : [push]
jobs:
  say-hello:
    runs-on: ubuntu-latest
    - name : say hello
      run : echo "Hello World"
    - name : first github 
      run : echo "this is first github actions"
      
