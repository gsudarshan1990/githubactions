name : Hello World
on : push
jobs:
  say-hello :
    runs-on: ubuntu-latest
    steps:
      - name: Say hello world
        run : echo "Hello World"
      - name: : Say github actions
        run: echo "This is first github actions"
